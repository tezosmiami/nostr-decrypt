import { Event } from 'nostr-tools';
import { Config } from '../types';
declare const useSubscribe: ({ filters, relays, options }: Config) => {
    events: Event[];
    eose: boolean;
    invalidate: () => void;
    loadMore: () => void;
};
export default useSubscribe;
