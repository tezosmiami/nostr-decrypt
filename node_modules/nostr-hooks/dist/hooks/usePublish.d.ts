import { Event, EventTemplate } from 'nostr-tools';
declare const usePublish: (relays: string[], privateKey?: string) => (partialEvent: Partial<EventTemplate>) => Promise<Event>;
export default usePublish;
